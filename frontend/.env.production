# Production API base URL — baked into the Next.js build (NEXT_PUBLIC_* is public).
# This guarantees the frontend calls the Coolify backend, regardless of the
# Coolify env UI. Dev still uses .env.local (localhost).
NEXT_PUBLIC_API_URL=https://api.careopsx.co.in
